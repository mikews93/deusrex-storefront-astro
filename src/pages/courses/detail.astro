---
/**
 * Course detail — single dynamic-shell template. No /api/v1/courses/:id
 * endpoint exists today (T004 audit); the island filters the list response
 * client-side.
 */
import SubPageLayout from '../../layouts/SubPageLayout.astro';
import CatalogDetail from '../../islands/CatalogDetail.tsx';
import i18n from '../../i18n';
---

<SubPageLayout title={i18n.t('storefront.pages.courseDetail')}>
  <CatalogDetail client:load section="courses" backHref="/courses" />
</SubPageLayout>
