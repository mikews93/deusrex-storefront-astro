---
/**
 * Checkout success page — SSR shell + React Island for cart cleanup.
 */
import SubPageLayout from '../../layouts/SubPageLayout.astro';
import CheckoutResult from '../../islands/CheckoutResult.tsx';

const orgSlug = import.meta.env.ORG_SLUG;
const apiUrl = import.meta.env.BACKEND_API_URL;
---

<SubPageLayout title="Order Confirmed">
  <div class="sf-container">
    <CheckoutResult
      client:load
      status="success"
      orgSlug={orgSlug}
      apiUrl={apiUrl}
    />
  </div>
</SubPageLayout>
