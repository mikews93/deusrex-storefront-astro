---
/**
 * Home page — prerendered (SSG).
 * AI-generated sections are injected here by AstroSiteGeneratorService.
 * This file is REPLACED at build time with generated content.
 *
 * The placeholder below shows the structure; the real file will contain
 * the actual AI-generated HTML sections from the template bundle.
 */
export const prerender = true;

import BaseLayout from '../layouts/BaseLayout.astro';
import Header from '../components/Header.astro';
import Footer from '../components/Footer.astro';
// [GENERATED] Section imports will be injected here
---

<BaseLayout>
  <Header />

  <!-- [GENERATED] AI-generated sections will be injected here -->
  <main>
    <section id="hero" class="sc-hero">
      <div class="sc-hero__container">
        <h1 class="sc-hero__title">Welcome</h1>
        <p class="sc-hero__subtitle">Your website is being built...</p>
      </div>
    </section>
  </main>

  <Footer />
</BaseLayout>
